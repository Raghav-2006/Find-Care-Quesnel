# Google Maps API key (required for map functionality)
VITE_GOOGLE_MAPS_API_KEY=

# OpenAI API key (optional, server-side only — enables AI symptom analysis)
# If not set, the app uses a built-in rule-based fallback
OPENAI_API_KEY=
